#!/usr/bin/python
import os
from sys import argv

print argv
if len(argv) < 2:
    print ("I need a commit message!")
        exit()

	os.system("git add -A")
	os.system('git commit -m "' + argv[1] +'"')
	os.system('git push')

